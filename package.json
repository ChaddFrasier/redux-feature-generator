{
  "name": "redux-feature-generator",
  "version": "0.0.1",
  "description": "Generate redux features including their folders and files that follow the template set by the `react-app --template redux-typescript` API",
  "main": "./dist/cli.js",
  "scripts": {
    "test": "yarn test",
    "build": "npx tsc",
    "test:gen": "generate-feature componentName --template redux-typescript"
  },
  "bin": {
    "generate-feature": "./dist/cli.js"
  },
  "keywords": [],
  "author": "ChaddFrasier",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "^17.0.14",
    "@types/yargs": "^17.0.8",
    "typescript": "^4.5.5",
    "yargs": "^17.3.1"
  }
}
